if (file.exists('~/.Rprofile')) sys.source('~/.Rprofile', envir = environment())

options(
  blogdown.method = 'custom', digits = 4, servr.daemon = TRUE, formatR.indent = 2,
  blogdown.publishDir = "../jmcimula.github.io", blogdown.yaml.empty = FALSE
)

 
